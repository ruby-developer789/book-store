<table class="table table-hover table-bordered">
  <tr>
    <th>Order ID</th>
    <th>Created At</th>
    <th>Price</th>
  </tr>
  <% if @orders.present? %>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.created_at.strftime("%d-%m-%Y %l:%S %p") %></td>
        <td>$<%= order.total_price %></td>
      </tr>
    <% end %>
  <% else %>
    <tr>
      <td colspan="4" class="text-center">No Orders</td>
    </tr>
  <% end %>
</table>